# Story 2.3: Consultation Session Service — Acúmulo de Transcript em Memória

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, build, coderabbit]

## Story

**As a** board (e futuros motores E4/E5),
**I want** um serviço de sessão de consulta que consuma o stream do STT e acumule o transcript em memória,
**so that** exista um estado canônico da transcrição por sessão (ADR-005) que UI e motores possam consumir.

## Acceptance Criteria

1. Existe um `ConsultationSessionService` que consome `SttSession` (AsyncIterable de `TranscriptSegment`) e mantém o transcript acumulado da sessão **em memória** (ADR-005 — runtime validado na POC E3).
2. Segmentos **parciais** atualizam/substituem a "ponta" provisória; segmentos **finais** são acrescentados de forma imutável ao acumulado — sem duplicação.
3. O serviço expõe leitura do estado (transcript acumulado + parcial corrente) e um mecanismo de assinatura/notificação de novos segmentos (consumido pela 2.4 e, depois, E3/E4).
4. Início de sessão exige consulta autorizada (gate da Story 1.4 verificado no servidor antes de ligar o consumo).
5. Encerramento limpo: parar a sessão fecha o stream (`close()`), preserva o acumulado em memória e não vaza listeners/loops.
6. Erro do STT muda o estado da sessão para `degraded` (insumo da 2.6) sem derrubar a sessão.
7. Testes: acúmulo parcial/final, ordem, não-duplicação, degradação e encerramento — com o **fake STT determinístico da Story 1.6** (REUSE).

## Tasks / Subtasks

- [ ] **Task 1 — Serviço de sessão** (AC: 1, 2, 3)
  - [ ] Estado por `consultationId`: acumulado (finais) + parcial corrente + status (`live|paused|degraded|ended`)
  - [ ] Loop consumidor do AsyncIterable; pub/sub interno de segmentos
- [ ] **Task 2 — Gate + ciclo de vida** (AC: 4, 5)
  - [ ] `assertCaptureAuthorized` antes de iniciar; stop fecha stream e finaliza loop
- [ ] **Task 3 — Estado degradado** (AC: 6)
  - [ ] Captura de erro do stream → `degraded` + notificação (2.6 consome)
- [ ] **Task 4 — Testes** (AC: 7)
  - [ ] Suíte com FakeSttProvider (1.6): parciais→final, erro→degraded, stop limpo

## Dev Notes

> Fontes: architecture §4/§5 (Consultation Session Service, contrato STT), ADR-005 (stateful por sessão — Proposto, POC valida); epic-02 IN; Story 1.6 (fake = REUSE), Story 1.4 (gate).

- **Runtime ainda não decidido** (ADR-005): implementar como classe/módulo Node puro, sem amarrar a serverless/edge — a decisão vem da POC E3. Sem persistência de transcript neste story (E9 trata documentação clínica).
- Transcript contém dado de saúde: enquanto for só memória, NFR9 não exige cifra; **não** logar conteúdo do transcript.
- Auditoria (1.5) é para **escritas clínicas** (contribuições, E4+) — acúmulo em memória não audita.

### O que NÃO fazer

- Não persistir transcript em banco (E9); não criar WebSocket do board (E3/ADR-003); não implementar triggers (E4).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-11 | v0.1 | Draft inicial a partir do E2 (Stories Candidatas #3) | River (@sm) |
| 2026-06-11 | v0.2 | Validated GO (9/10 — sem estimativa de pontos explícita, aceito: tamanho herdado do épico L) — Status: Draft → Ready | Pax (@po) |

## Dev Agent Record

### Agent Model Used

_(a preencher pelo @dev)_

### Debug Log References

_(a preencher pelo @dev)_

### Completion Notes List

_(a preencher pelo @dev)_

### File List

_(a preencher pelo @dev)_

## QA Results

_(a preencher pelo @qa)_
