# Story 1.4: Consent Service — Consentimento de Gravação (FR20)

## Status

Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, build, coderabbit]

## Story

**As a** nutrólogo iniciando uma consulta com o board,
**I want** que o consentimento de gravação seja capturado e persistido antes de qualquer captura de áudio,
**so that** nenhuma gravação ocorra sem base legal explícita (FR20/LGPD), protegendo paciente, médico e o produto.

## Acceptance Criteria

1. O consentimento de gravação é **exigido e persistido antes** de qualquer captura de áudio; **sem consentimento, a gravação é bloqueada** (ref. Critério de Aceitação 3 do E1 / FR20).
2. O consentimento é persistido como entidade `CONSENT` vinculada à `CONSULTATION` (architecture §8: `CONSULTATION ||--|| CONSENT`), com carimbo de tempo e identidade de quem consentiu/registrou.
3. O backend é a **fonte de verdade**: a captura de áudio só é autorizada por um gate de servidor que verifica consentimento válido para aquela consulta (não confiar apenas no cliente).
4. A ausência ou revogação de consentimento impede o início (e interrompe) a captura, com feedback claro na UI.
5. O registro de consentimento é auditável (qual consulta, quando, por quem) — alinhado à fundação de auditoria (NFR10).
6. Nenhum áudio é capturado, transmitido ou persistido enquanto o gate de consentimento não aprovar.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: API (serviço de consentimento + gate de servidor)
**Secondary Type(s)**: Security/Compliance (LGPD/FR20), Frontend (captura de consentimento)
**Complexity**: Medium

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @architect (quality gate — valida o gate servidor como fonte de verdade)

**Supporting Agents**:
- @data-engineer (usa a tabela CONSENT da Story 1.3)
- @devops (push/PR exclusivo)
- @qa (validação de story; foco no bloqueio sem consentimento)

### Quality Gate Tasks

- [~] Pre-Commit (@dev): diferido — CodeRabbit CLI exige `auth login` interativo (WSL); rodar no pre-PR
- [ ] Pre-PR (@devops): rodar antes de criar o pull request
- [ ] Pre-Deployment (@devops): N/A nesta story

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações), HALT se persistir
- HIGH issues: auto_fix na 1ª iteração, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Security/Compliance: enforcement do consentimento no servidor (não burlável pelo cliente)
- Error handling: bloqueio claro quando não há consentimento

**Secondary Focus**:
- Validação de input no endpoint de consentimento
- Auditabilidade do registro de consentimento

## Tasks / Subtasks

- [x] **Task 1 — Persistência do consentimento** (AC: 2, 5)
  - [x] Implementar gravação de `CONSENT` vinculado a `CONSULTATION` (usar tabela da Story 1.3)
  - [x] Registrar timestamp + identidade (do `USER` autenticado, Story 1.2)
- [x] **Task 2 — Gate de servidor (fonte de verdade)** (AC: 1, 3, 6)
  - [x] Endpoint/serviço que autoriza captura **apenas** com consentimento válido para a consulta
  - [x] Garantir que nenhum caminho de captura/transmissão de áudio exista sem passar pelo gate (AC 6)
- [x] **Task 3 — Revogação/ausência** (AC: 4)
  - [x] Tratar ausência de consentimento (bloqueia início) e revogação (interrompe captura)
  - [x] Feedback de UI claro nos dois casos
- [x] **Task 4 — UI de consentimento** (AC: 1, 4)
  - [x] Componente que solicita e confirma consentimento antes de habilitar a gravação (Tailwind)
- [x] **Task 5 — Testes** (AC: 1, 3, 6)
  - [x] Teste: sem consentimento → captura bloqueada (servidor nega)
  - [x] Teste: com consentimento válido → captura autorizada
  - [x] Teste: revogação interrompe a autorização

## Dev Notes

> Fontes: `docs/prd.md` FR20; `docs/architecture.md` §8 (CONSENT/CONSULTATION), §3 (Audit & Consent Service), §11 (LGPD by design), ADR-006; `docs/epics/epic-01-fundacao-compliance.md` (IN: Consent Service FR20).

### Contexto de arquitetura

- **Módulo (architecture §3):** "Audit & Consent Service — Consentimento de gravação; trilha de auditoria de toda contribuição (defesa CFM). FR19, FR20, NFR9, NFR10." Esta story entrega a **parte de consentimento**; a trilha de auditoria com proveniência é a Story 1.5.
- **Modelo (§8):** `CONSULTATION ||--|| CONSENT : exige` — relação 1:1. A tabela `CONSENT` vem da Story 1.3.
- **Princípio:** compliance-by-design (ADR-006) — consentimento é **gate**, não checkbox cosmético. O servidor decide (AC 3), nunca confiar só no cliente.

### Dependências

- **Depende de 1.2** (identidade `USER` para registrar quem consentiu) e **1.3** (tabela `CONSENT`/`CONSULTATION`).
- **Desbloqueia E2** (Pipeline de Transcrição): a captura de áudio do E2 só pode ligar atrás deste gate.

### Fronteira de escopo

- A **captura/streaming de áudio real** é do E2 — aqui implementamos o **gate** que o E2 vai respeitar. Garantir que o contrato do gate esteja claro para o E2 consumir.
- Não implementar STT nem transporte de áudio (E2).

### O que NÃO fazer

- Não construir a trilha de auditoria completa (Story 1.5).
- Não implementar disclaimers de UI "IA assiste, médico decide" (FR19 → Story 1.7).
- Não decidir retenção/base legal de áudio (consultoria jurídica → Story 1.8).

### Boundary de agentes

- `git push`/PR = **@devops exclusivo**. Operações de schema, se necessárias, coordenar com @data-engineer (a tabela já vem da 1.3).

### Testing

- **Foco crítico (AC 1/3/6):** provar por teste que **sem consentimento não há captura** e que o enforcement é no servidor. Esse é o coração de compliance da story.
- **Framework/localização:** padrão da Story 1.1.
- Testar revogação e ausência como estados de primeira classe.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-09 | v0.1 | Draft inicial a partir do E1 (Stories Candidatas #4) | River (@sm) |
| 2026-06-10 | v0.2 | Validated GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-06-10 | v0.3 | Início da implementação — Status: Ready → InProgress | Dex (@dev) |
| 2026-06-10 | v1.0 | Implementação completa (5 tasks, 9 testes, 4 gates PASS) — Status: InProgress → Ready for Review | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Dex / @dev)

### Debug Log References

- `npm run lint` → PASS (eslint, 0 erros)
- `npm run typecheck` → PASS (8/8 projetos)
- `npm test` → PASS (39/39 testes; +9 do consent, sem regressão)
- `npm run build` → PASS (rotas `/consultations/[id]` e `/api/consultations/[id]/capture-authorization` geradas)
- CodeRabbit pre-commit → diferido (CLI exige `auth login` interativo via WSL)

### Completion Notes List

- **Consent Service como gate de servidor (AC1/AC3/AC6):** novo package `@nutrimed/consent`. O servidor é a única fonte de verdade — `isCaptureAuthorized`/`assertCaptureAuthorized` negam por default; nenhum caminho de captura do E2 pode ligar sem passar por esse gate. Default sempre NEGA (consent criado com `granted=false` na abertura da consulta).
- **Persistência + auditabilidade (AC2/AC5):** `CONSENT` 1:1 com `CONSULTATION` (tabela da Story 1.3), `grant` registra `granted_by` (identidade do USER da Story 1.2) + `granted_at`. Rótulo do paciente cifrado em repouso (`@nutrimed/crypto`, NFR9).
- **Revogação/ausência (AC4):** `revokeConsent` faz o gate voltar a negar; UI mostra dois estados claros (🟢 autorizada / 🔒 bloqueada) com ação correspondente.
- **Gate HTTP (AC3/AC6):** rota `GET /api/consultations/[id]/capture-authorization` → 401 (sem auth) / 403 `consent_required` / 200 `{authorized:true}`. É o contrato que o E2 consome antes de capturar.
- **Decisões:** package isolado (espelha `@nutrimed/auth`) → reutilizável fora da web; testes via PGlite (sem Docker), provando por teste o coração de compliance (sem consentimento → sem captura). Captura/streaming real fica para o E2 (fora de escopo).
- **Fronteiras respeitadas:** trilha de auditoria completa = 1.5; disclaimers FR19 = 1.7; base legal/retenção = 1.8. Nenhum `git push`/PR (exclusivo @devops).

### File List

**Criados:**
- `packages/consent/package.json`
- `packages/consent/tsconfig.json`
- `packages/consent/src/index.ts`
- `packages/consent/src/consent.ts`
- `packages/consent/src/consent.test.ts`
- `apps/web/lib/crypto-key.ts`
- `apps/web/lib/consent-actions.ts`
- `apps/web/app/consultations/[id]/page.tsx`
- `apps/web/app/api/consultations/[id]/capture-authorization/route.ts`

**Modificados:**
- `apps/web/package.json` (dependência `@nutrimed/consent`)
- `apps/web/app/page.tsx` (form "Iniciar consulta" no painel)
- `docs/stories/1.4.consent-service-gravacao.md` (este arquivo)

## QA Results

_(a preencher pelo @qa)_
