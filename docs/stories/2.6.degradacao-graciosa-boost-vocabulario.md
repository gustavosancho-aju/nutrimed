# Story 2.6: Degradação Graciosa + Boost de Vocabulário Clínico

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, build, coderabbit]

## Story

**As a** nutrólogo em consulta,
**I want** que falhas de transcrição degradem graciosamente (banner, sem travar) e que termos clínicos sejam reforçados no STT,
**so that** a consulta nunca quebra por causa do board (frontend-spec §3.1) e os gatilhos futuros recebam texto confiável (T4).

## Acceptance Criteria

1. Falha do STT em runtime → estado `degraded` da sessão (Story 2.3) vira **banner discreto "transcrição instável"** no topo da área principal; a consulta **não trava** (frontend-spec §3.1, Critério 5 do E2).
2. Recuperação automática quando possível (retry/reabertura de stream com backoff limitado); sucesso remove o banner e retoma o acúmulo sem duplicar segmentos.
3. Existe mecanismo de **boost/vocabulário de termos clínicos** aplicado ao abrir o stream (quando o vendor suportar — campo de opções no adapter), com a lista de termos versionada e derivada das KBs das personas.
4. O `<TranscriptPanel>` (2.4) reflete o estado `erro-transcrição` de forma consistente com o banner (uma única fonte de estado — a sessão 2.3).
5. Testes: erro→banner→recuperação→sem duplicação (com fake STT que falha de forma determinística); aplicação do vocabulário verificada no contrato do adapter.

## Tasks / Subtasks

- [ ] **Task 1 — Estado degradado fim-a-fim** (AC: 1, 4)
  - [ ] Banner no chrome da consulta dirigido pelo status da sessão
- [ ] **Task 2 — Retry/backoff** (AC: 2)
  - [ ] Reabertura limitada (ex.: 3 tentativas, backoff exponencial), retomada sem duplicar finais
- [ ] **Task 3 — Vocabulário clínico** (AC: 3)
  - [ ] Lista versionada (fonte: personas-knowledge-base-seed.md) + passagem via opções do `ISttProvider`/adapter
- [ ] **Task 4 — Testes** (AC: 5)

## Dev Notes

> Fontes: frontend-spec §3.1 (edge case "transcrição instável"); epic-02 IN (degradação + boost); PRD NFR11; risco T4; Story 2.5 (mede o efeito do boost).

- Depende de 2.1 (adapter), 2.3 (estado da sessão), 2.4 (panel). É a story de fechamento do pipeline.
- Se a interface `ISttProvider` precisar de campo de opções p/ vocabulário, a mudança é **aditiva** no contrato (1.6) — coordenar com @architect (gate), sem quebrar fakes existentes.
- Board "sem novas sugestões" durante degradação (frontend-spec) só se materializa no E3+ — aqui basta o estado/baner.

### O que NÃO fazer

- Não implementar lógica de board/gatilhos; não persistir transcript; não escolher vendor (2.5).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-11 | v0.1 | Draft inicial a partir do E2 (Stories Candidatas #6) | River (@sm) |
| 2026-06-11 | v0.2 | Validated GO (9/10 — sem estimativa de pontos explícita, aceito: tamanho herdado do épico L) — Status: Draft → Ready | Pax (@po) |

## Dev Agent Record

### Agent Model Used

_(a preencher pelo @dev)_

### Debug Log References

_(a preencher)_

### Completion Notes List

_(a preencher)_

### File List

_(a preencher)_

## QA Results

_(a preencher pelo @qa)_
