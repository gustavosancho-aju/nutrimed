# Story 12.9: Telemetria de Custo/Uso + Runbook de Deploy do Canal

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@devops"
quality_gate_tools: [lint, typecheck, test, build]

## PO Validation (Pax)

**Verdict: GO (8/10)** — observabilidade (NFR7) reusando `@nutrimed/telemetry` (E10), métricas **sem PII**, runbook de secrets/setWebhook e `.env.example`. Gate por @devops (secrets/deploy é fronteira @devops). Depende do canal ativo (12.7). Sem bloqueios. Encerra o épico.

## Story

**As a** operador do piloto,
**I want** observar o custo de visão por foto e o uso do canal, e ter um runbook de deploy do webhook,
**so that** o canal do paciente seja monitorável (NFR7) e operável em produção com segurança.

## Acceptance Criteria

1. O **custo de visão por foto** é capturado via `onUsage` do `ClaudeFoodEstimator` e registrado na telemetria (padrão E10 / NFR7).
2. **Métricas de uso do canal** (agregadas, sem PII): nº de fotos/dia, nº de pacientes ativos no canal.
3. `docs/deploy/RUNBOOK.md` ganha a seção **Telegram**: `fly secrets set TELEGRAM_BOT_TOKEN=… TELEGRAM_WEBHOOK_SECRET=…`, registro do `setWebhook`, e passo de rollback (remover webhook).
4. `.env.example` atualizado com as novas vars (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_MODE`, `PUBLIC_BASE_URL`, `FOOD_ESTIMATOR`, `PAIRING_CODE_TTL_MIN`).
5. **Verificação**: enviar uma foto (dev/fake ou real) e ver custo/uso refletidos na telemetria.

## Tasks / Subtasks

- [x] **Task 1 — Custo por foto** (AC 1): ligar `onUsage` do estimador ao `@nutrimed/telemetry`.
- [x] **Task 2 — Uso do canal** (AC 2): métricas agregadas (sem PII).
- [x] **Task 3 — Runbook + env** (AC 3,4): seção Telegram no RUNBOOK + `.env.example`.
- [x] **Task 4 — Verificação** (AC 5) + evidência.

## Dev Notes

> Reusar `@nutrimed/telemetry` (E10). **Nunca** logar conteúdo clínico/PII na telemetria (NFR9) — só métricas agregadas e custo. Secrets via `fly secrets set` (não versionar). Push/PR/secrets em produção = **@devops exclusivo**.

### O que NÃO fazer
- Não logar valores nutricionais por paciente, nomes, `chat_id` cru ou a foto. Sem novas portas/processos no Fly (webhook na 3000).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-01 | v0.1 | Draft inicial (E12 Fase 6, story 9) | River (@sm) |
| 2026-07-01 | v0.2 | Validated GO (8/10) — Draft → Ready | Pax (@po) |
| 2026-07-01 | v1.0 | TelegramTelemetry (custo de visão via onUsage + fotos/pacientes, sem PII) + wiring no runtime + seção Telegram no RUNBOOK + .env.example; 4 gates PASS — Ready → InReview | Dex (@dev) |
| 2026-07-01 | gate | Quality gate PASS — InReview → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (Claude Code) — Dex (@dev), YOLO

### Completion Notes List
- `@nutrimed/telemetry` ganhou `TelegramTelemetry` (reusa `PRICING` do board — custo de visão consistente): `photoLogged(chatId, dayISO)` (fotos + pacientes ativos distintos + fotos/dia), `visionUsage(in, out)`, `visionUsd()`, `report()`. **Sem PII** (NFR9): só contadores/custo; `chat_id` só num Set p/ contar distintos (nunca exposto).
- `createFoodEstimator(env, { onUsage })` agora repassa `onUsage` ao `ClaudeFoodEstimator` (fake não tem custo). O `telegram-runtime` liga `onUsage → telemetry.visionUsage` e chama `telemetry.photoLogged` por foto, com log `[telegram] foto processada — pacientes ativos: N, custo de visão acumulado ~US$X` (sem conteúdo).
- `TelegramRuntime` expõe `telemetry` (base p/ um painel/endpoint admin futuro).
- **RUNBOOK** (`docs/deploy/RUNBOOK.md`): nova "Fase 5 — Canal Telegram" (rotação do token, `fly secrets` de `TELEGRAM_BOT_TOKEN`/`TELEGRAM_WEBHOOK_SECRET`, envs não-sensíveis, `setWebhook` automático + `getWebhookInfo`, teste E2E, rollback via `deleteWebhook`, gate CJ-12). **`.env.example`**: bloco E12 documentado.
- 3 testes de `TelegramTelemetry` (contagem/distintos/por-dia, custo via PRICING, zerado). Gates: lint ✅ · typecheck ✅ · test ✅ (272 pass + 1 skip) · build ✅.

### File List
**Criados:** `packages/telemetry/src/telegram-telemetry.ts`, `packages/telemetry/src/telegram-telemetry.test.ts`
**Modificados:** `packages/telemetry/src/index.ts`, `packages/food-vision/src/index.ts`, `apps/web/lib/telegram-runtime.ts`, `docs/deploy/RUNBOOK.md`, `.env.example`

## QA Results

**Gate: PASS** (2026-07-01, orquestrado @qa). Custo de visão reusa `PRICING` (consistência NFR7); telemetria **sem PII** (só contadores/custo; `chat_id` não exposto — NFR9); `onUsage` plugado sem quebrar o fake; runbook e `.env.example` completos, com gate CJ-12 explícito. 3 testes; 4 gates verdes. Sem CRITICAL/HIGH. **Épico E12 completo (9/9).**
