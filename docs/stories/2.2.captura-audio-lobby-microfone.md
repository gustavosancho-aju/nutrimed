# Story 2.2: Captura de Áudio + Checagem de Microfone no Lobby

## Status

Ready for Review

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, build, coderabbit]

## Story

**As a** nutrólogo iniciando uma consulta,
**I want** que o lobby verifique meu microfone e capture o áudio da consulta só após tudo OK,
**so that** a transcrição comece confiável em < 30s e nenhum áudio seja capturado sem autorização (FR1, FR20, frontend-spec §3.1).

## Acceptance Criteria

1. O lobby de consulta checa permissão/disponibilidade de microfone **antes** de permitir "Iniciar consulta" (frontend-spec §3.1).
2. Sem permissão de microfone, o início é **bloqueado** com instrução clara e botão "tentar de novo" (Critério 6 do E2).
3. A captura de áudio (getUserMedia/AudioWorklet ou equivalente) só inicia após (a) microfone OK **e** (b) gate de consentimento do servidor autorizar (`GET /api/consultations/[id]/capture-authorization` — Story 1.4). Cliente nunca decide sozinho.
4. O áudio capturado é entregue ao adapter `ISttProvider` (Story 2.1) no formato/encoding que o stream exige; nenhum áudio é persistido neste story.
5. Encerrar/revogar consentimento interrompe a captura (tracks paradas), sem travar a página.
6. Testes: lógica de gating do cliente testada (estados sem-permissão/sem-consentimento/ok); chamada ao gate de servidor verificada.

## Tasks / Subtasks

- [x] **Task 1 — Lobby com checagem de microfone** (AC: 1, 2)
  - [x] Tela/etapa de lobby na rota da consulta com estados: checando / sem-permissão (instrução + retry) / OK
- [x] **Task 2 — Gating duplo (mic + consentimento de servidor)** (AC: 3)
  - [x] Consultar capture-authorization antes de abrir captura; refletir 401/403/200
- [x] **Task 3 — Captura → adapter STT** (AC: 4, 5)
  - [x] Pipeline de áudio do navegador ao stream do provider; stop limpo em revogação/saída
- [x] **Task 4 — Testes** (AC: 6)
  - [x] Testes de UI/lógica dos estados do lobby (jsdom, padrão Story 1.7) e do gating

## Dev Notes

> Fontes: frontend-spec §3.1 (fluxo lobby/mic, edge cases); epic-02 IN (captura + checagem de microfone); Story 1.4 (gate FR20 — contrato consumido aqui).

- O gate de servidor é a **fonte de verdade** (Story 1.4): a UI apenas reflete. Caminho de captura DEVE passar por `assertCaptureAuthorized`/rota de autorização.
- Toggles ativar/silenciar doutor do lobby (§3.1) são do **E7** — aqui só microfone + iniciar.
- APIs de mídia do navegador não existem no jsdom — isolar atrás de interface fina mockável.

### O que NÃO fazer

- Não persistir áudio; não implementar TranscriptPanel (2.4) nem session service (2.3); não construir board/toggles de persona (E7).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-11 | v0.1 | Draft inicial a partir do E2 (Stories Candidatas #2) | River (@sm) |
| 2026-06-11 | v0.2 | Validated GO (9/10 — sem estimativa de pontos explícita, aceito: tamanho herdado do épico L) — Status: Draft → Ready | Pax (@po) |
| 2026-06-11 | v0.3 | Status: Ready → InProgress | Dex (@dev) |
| 2026-06-11 | v1.0 | Implementação completa (9 testes) — Status: InProgress → Ready for Review | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-fable-5 (Claude Code) — executor @dev (Dex), YOLO

### Debug Log References

- 9 testes (lobby + microphone) PASS; suíte total 104/104; lint/typecheck(11/11)/build PASS.
- Infra: alias `@/` do Next espelhado no vitest (resolve.alias).

### Completion Notes List

- `apps/web/lib/microphone.ts`: `checkMicrophone` (ok|denied|unavailable; reusa o stream do check na captura — pede permissão 1x) e `createAudioSource` (MediaRecorder timeslice 250ms → AsyncIterable<Uint8Array> p/ `SttOpenOptions.audio` da 2.1). APIs de mídia atrás de interfaces finas mockáveis (jsdom não as tem).
- `<ConsultationLobby>`: GATING DUPLO — etapa mic (checando/sem-permissão+instrução+retry/ok) e etapa servidor (`GET .../capture-authorization` da 1.4; 401/403 bloqueiam). "Iniciar consulta" só habilita com ambos OK; ao iniciar entrega o `AudioSource` via `onStart` (AC3/AC4).
- `AudioSource.stop()` para recorder + tracks e encerra o iterable, idempotente (AC5 — revogação/saída).
- Nenhum áudio persistido; wiring fim-a-fim (lobby→sessão 2.3→adapter 2.1 na página real) é o walking skeleton do E3.

### File List

- `apps/web/lib/microphone.ts` (novo)
- `apps/web/components/consultation-lobby.tsx` (novo)
- `apps/web/components/consultation-lobby.test.tsx` (novo — 9 testes)
- `vitest.config.ts` (alias @/)

## QA Results

_(a preencher pelo @qa)_
