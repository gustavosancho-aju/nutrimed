# Story 11.7: Dashboard — Abas Geral + Bioimpedância (entrada manual + evolução)

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, build]

## PO Validation (Pax)

**Verdict: GO (9/10)** — reúsa primitivas da 11.6; campos exatamente os especificados; entrada manual é o caminho primário (degradação graciosa antes do PDF). Sem bloqueios.

## Story

**As a** nutrólogo,
**I want** as abas Geral e Bioimpedância mostrando valor atual, variação e evolução, com entrada manual,
**so that** eu acompanhe e apresente a composição corporal do paciente mesmo sem importar PDF.

## Acceptance Criteria

1. **Aba Geral:** Peso · Massa Muscular · % Gordura — cada um com valor atual + variação vs. anterior + mini-tendência (`<MetricCard>`), e o **Principal Objetivo em destaque**.
2. **Aba Bioimpedância:** Peso · Massa Muscular · Massa de Gordura · Cintura Abdominal · IMC · PGC — cada um com `<TrendChart>` de evolução.
3. **Entrada manual** (form): data da medição + os 6 campos (todos opcionais — medição parcial válida) → `addMeasurementAction` (11.6) grava em `body_composition`.
4. Unidades exibidas (kg, %, cm); medições ordenadas por data; estado vazio tratado.
5. Design premium; navegação por teclado.

## Tasks / Subtasks

- [x] **Task 1 — Aba Geral** (AC 1): 3 `<MetricCard>` + objetivo em destaque
- [x] **Task 2 — Aba Bioimpedância** (AC 2, 4): 6 métricas com `<TrendChart>`
- [x] **Task 3 — Form de entrada manual** (AC 3): data + 6 campos → `addBodyComposition`
- [x] **Task 4 — Verificação** (AC 1–4): Playwright — lançou 2 medições, variação ▼5kg e gráficos OK

## Dev Notes

> Blob `BodyCompositionValues` = `{ peso, massaMuscular, massaGordura, cintura, imc, pgc }` (11.2). Reúsa `<MetricCard>`/`<TrendChart>`/`computeTrend` (11.6).

### O que NÃO fazer
- Exames são a 11.8. Importação de PDF é a Fase 4 (aqui só entrada manual).

### Testing
- Verificação ao vivo (Playwright): inserir medições e conferir variação + render do gráfico.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-23 | v0.1 | Draft inicial (E11 Fase 3, story 7) | River (@sm) |
| 2026-06-23 | v0.2 | Validated GO (9/10) — Draft → Ready | Pax (@po) |
| 2026-06-23 | v1.0 | Abas Geral + Bioimpedância (MetricCards + TrendCharts + form 6 campos); verificado ao vivo — InProgress → InReview | Dex (@dev) |
| 2026-06-23 | gate | Quality gate PASS — Status: InReview → Done | Aria (@architect) |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (Claude Code) — Dex (@dev), YOLO
### Completion Notes List
- Aba Geral: 3 `<MetricCard>` (Peso/Massa Muscular/% Gordura) + objetivo em destaque. Aba Bioimpedância: 6 métricas com `<TrendChart>` + `<MeasurementForm>` (6 campos, medição parcial válida). Implementadas em `app/patients/[id]/dashboard/page.tsx` (compartilha o shell da 11.6).
- Verificado ao vivo: 2 medições → Peso 90kg ▼5kg (-5.3%), Massa Muscular 35kg ▲1kg, % Gordura 28% ▼4%, com gráficos SVG.
### File List
**Modificados:** `apps/web/app/patients/[id]/dashboard/page.tsx` (abas Geral/Bioimpedância); reúsa `<MetricCard>`/`<TrendChart>`/`<MeasurementForm>` da 11.6.
## QA Results
**Gate: PASS** (2026-06-23, Aria (@architect)). Campos exatamente os especificados; entrada manual cifrada+auditada; verificado ao vivo (screenshots `verify-f3-geral/bioimpedancia.png`). Gates do monorepo PASS. Sem CRITICAL/HIGH.
