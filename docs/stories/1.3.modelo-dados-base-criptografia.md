# Story 1.3: Modelo de Dados Base + Criptografia (Repouso/Trânsito)

## Status

Done

## Executor Assignment

executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: [lint, typecheck, test, build, coderabbit, migration-check]

## Story

**As a** sistema NutriMed que manipula dados sensíveis de saúde,
**I want** o modelo de dados base (USER, CONSULTATION, CONSENT, AUDIT_LOG) persistido com criptografia em repouso e em trânsito,
**so that** dados clínicos sejam armazenados com segurança jurídica (LGPD/NFR9) desde o primeiro registro, sustentando consentimento e auditoria.

## Acceptance Criteria

1. Existe um schema Postgres com as entidades base **USER, CONSULTATION, CONSENT, AUDIT_LOG** e seus relacionamentos conforme `architecture.md` §8 (ref. Critério de Aceitação 4 do E1).
2. Dados de saúde são criptografados **em repouso** e **em trânsito** (NFR9), de forma **verificável por teste automatizado** (não só por inspeção — mitigação de T7).
3. As migrations são versionadas, reproduzíveis e aplicáveis do zero (idempotência do estado final).
4. A conexão com o banco força TLS/SSL (criptografia em trânsito).
5. Campos sensíveis (dados de saúde / PII) usam criptografia em repouso (coluna/campo cifrado ou cripto no nível de storage), documentada e testada.
6. O modelo respeita as restrições de integridade dos relacionamentos de §8: `USER 1—N CONSULTATION`, `CONSULTATION 1—1 CONSENT`, e a fundação para `AUDIT_LOG` (proveniência detalhada é a Story 1.5).
7. Há um item de follow-up registrado para **residência de dados no Brasil** (LGPD), encaminhado para a Story 1.8 (ADR jurídico) — sem decidir aqui.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Database (schema, migrations, criptografia)
**Secondary Type(s)**: Security (proteção de dados em repouso/trânsito)
**Complexity**: High

### Specialized Agent Assignment

**Primary Agents**:
- @data-engineer (executor — DDL, migrations, estratégia de cripto)
- @dev (quality gate — integração com a app, testes de cripto)

**Supporting Agents**:
- @architect (decisão de residência de dados / encaminhar a 1.8)
- @devops (push/PR exclusivo; gestão de secrets de conexão de DB)
- @qa (validação de story e do teste de cripto)

### Quality Gate Tasks

- [ ] Pre-Commit (@data-engineer/@dev): rodar antes de marcar a story como Review
- [ ] Pre-PR (@devops): rodar antes de criar o pull request
- [ ] Pre-Deployment (@devops): rodar antes de aplicar migrations em ambiente compartilhado

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @data-engineer (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações), HALT se persistir
- HIGH issues: auto_fix na 1ª iteração, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Database: segurança de migration, constraints, foreign keys, índices
- Security: criptografia de dados em repouso/trânsito, nenhuma credencial de DB hardcoded

**Secondary Focus**:
- SQL injection / sanitização nas camadas de acesso
- Reprodutibilidade das migrations (aplicar do zero)

## Tasks / Subtasks

- [x] **Task 1 — Schema base** (AC: 1, 6)
  - [x] Definir DDL de `USER`, `CONSULTATION`, `CONSENT`, `AUDIT_LOG` (campos mínimos + chaves) conforme `architecture.md` §8
  - [x] Relacionamentos: `USER 1—N CONSULTATION`, `CONSULTATION 1—1 CONSENT`, `AUDIT_LOG` referenciável (proveniência completa fica na 1.5)
  - [x] Índices e constraints essenciais (PK/FK, NOT NULL onde aplicável)
- [x] **Task 2 — Migrations versionadas** (AC: 3)
  - [x] Configurar a ferramenta de migration do monorepo (alinhada à stack da Story 1.1)
  - [x] Migration inicial reproduzível do zero
- [x] **Task 3 — Criptografia em trânsito** (AC: 4)
  - [x] Forçar TLS/SSL na string/configuração de conexão; recusar conexão não cifrada
  - [x] Segredos de conexão via `.env` (nunca versionados)
- [x] **Task 4 — Criptografia em repouso** (AC: 2, 5)
  - [x] Definir e implementar a estratégia (campos cifrados para dados de saúde/PII e/ou cripto no nível de storage)
  - [x] Documentar quais campos são sensíveis e como são protegidos
- [x] **Task 5 — Testes verificáveis de cripto** (AC: 2, 5)
  - [x] Teste automatizado que comprova que um dado sensível **não** é legível em claro no storage e exige descriptografia válida
  - [x] Teste que comprova recusa de conexão sem TLS (ou verificação equivalente)
- [x] **Task 6 — Follow-up de residência de dados** (AC: 7)
  - [x] Registrar item para a Story 1.8 (ADR de residência BR) — não decidir aqui

## Dev Notes

> Fontes: `docs/architecture.md` §8 (modelo de dados), §11 (LGPD/CFM by design), ADR-006; `docs/epics/epic-01-fundacao-compliance.md` (IN: modelo de dados base + criptografia NFR9; risco T7).

### Modelo de dados (architecture §8 — relevante a esta story)

Entidades base e relacionamentos:
- `USER ||--o{ CONSULTATION : conduz`
- `CONSULTATION ||--|| CONSENT : exige`
- `CONTRIBUTION ||--o{ AUDIT_LOG : registra` (CONTRIBUTION/PERSONA/TRANSCRIPT_SEGMENT/CLINICAL_NOTE/KB_CHUNK **não** são desta story — entram em E2/E4/E5/E6/E9)

`AUDIT_LOG` (campos de §8, proveniência completa detalhada na Story 1.5):
- `id uuid`, `contribution_id uuid`, `kb_sources jsonb` ("proveniência — defesa CFM"), `model_version text`, `created_at timestamptz`

> ⚠️ Nesta story criamos a **fundação** de `AUDIT_LOG` (tabela + relacionamento). A lógica de gravação de auditoria com proveniência (gatilho, fontes KB, versão de modelo) é a **Story 1.5**.

### Requisitos NFR aplicáveis

- **NFR9 (architecture §8/§11):** "dados de saúde em repouso e em trânsito" criptografados; minimização; considerar residência BR.
- **NFR10:** auditabilidade — toda contribuição rastreia gatilho/fontes/versão de modelo (sustentado pela tabela criada aqui, lógica na 1.5).
- **T7 (riscos):** "validar criptografia e auditoria por teste automatizado, não só por inspeção" → **AC 2 é teste obrigatório**, não inspeção.

### Dependências e coordenação

- **Depende de 1.1** (monorepo + ferramenta de migration na stack).
- **Coordena com 1.2** (auth): a entidade `USER` é compartilhada — alinhar para não duplicar (IDS). Se 1.2 já definiu `USER` mínimo, esta story estende; senão, esta é a fonte de verdade do schema `USER`.
- **Desbloqueia 1.4** (CONSENT writes) e **1.5** (AUDIT_LOG writes com proveniência).

### O que NÃO fazer

- Não criar entidades de E2+ (TRANSCRIPT_SEGMENT, CONTRIBUTION, KB_CHUNK, etc.).
- Não decidir residência de dados (encaminhar à 1.8).
- Não implementar a lógica de auditoria/consent (apenas a tabela base de AUDIT_LOG).

### Boundary de agentes

- DDL/migrations/RLS = **@data-engineer**. `git push`/PR e aplicação de migrations em ambiente compartilhado = **@devops exclusivo**.

### Testing

- **Foco:** AC 2 e 5 — testes que **provam** a criptografia (dado ilegível em claro no storage; conexão sem TLS recusada). Esse é o coração da story (mitiga T7).
- **Migrations:** testar aplicação do zero (AC 3) — banco limpo → schema final sem erro.
- **Framework:** seguir o padrão da Story 1.1. Testes de DB podem usar container/efêmero local.
- **Nunca** versionar segredos de conexão; usar `.env.example`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-09 | v0.1 | Draft inicial a partir do E1 (Stories Candidatas #3) | River (@sm) |
| 2026-06-10 | v0.2 | Validated GO (10/10) — Status: Draft → Ready | Pax (@po) |
| 2026-06-10 | v0.3 | Implementação concluída (YOLO) — Status: Ready → InProgress → Ready for Review; schema base + AES-256-GCM + migrations (PGlite); 22 testes PASS | Dara (@data-engineer) |
| 2026-06-11 | gate | Quality gate CONCERNS — Status: Ready for Review → Done | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

Dara (@data-engineer) — Claude Opus 4.8 (1M context), `claude-opus-4-8`. Modo: YOLO.

### Debug Log References

- `pnpm install` → OK (+pg 8.21, @electric-sql/pglite 0.5.1, @types/pg).
- `pnpm test` → 22 testes PASS (crypto 7, db/connection 7, db/schema 6 via PGlite ~2.9s, domain 2).
- `pnpm typecheck` → 1ª falha em `crypto/aes-gcm.test.ts:29` (TS2532, `noUncheckedIndexedAccess`); corrigido (`?? 0`). 2ª → PASS (5 workspaces).
- `pnpm lint` / `pnpm build` → PASS.

### Completion Notes List

- **Abordagem (aprovada):** Postgres (architecture §8/§9) · migrations SQL puras + runner próprio · **testes sem Docker via PGlite** (Postgres WASM in-process) · cripto em repouso **AES-256-GCM em nível de aplicação**.
- **Pacotes criados:**
  - `@nutrimed/crypto` — AES-256-GCM (`encryptField`/`decryptField`/`loadEncryptionKey`/`generateEncryptionKey`). IV aleatório de 12 bytes, payload `base64(iv‖tag‖ciphertext)`, GCM tamper-evident. Chave de 32 bytes via `DATA_ENCRYPTION_KEY` (nunca versionada).
  - `@nutrimed/db` — schema (migration `0001_init.sql`), runner idempotente (`runMigrations` rastreia em `_migrations`), conexão com TLS obrigatório (`buildPgConfig`), executor `pg` (prod) e adaptador PGlite (testes).
- **Schema (architecture §8):** `app_user` ("user" é reservado), `consultation` (FK `user_id`, campo sensível `patient_label_enc` cifrado), `consent` (1:1 via `UNIQUE consultation_id`), `audit_log` (fundação — `contribution_id` sem FK pois CONTRIBUTION nasce em E4+; proveniência/imutabilidade completas na 1.5).
- **AC2/AC5 (cripto repouso, mitiga T7):** teste `schema.test.ts` insere dado de saúde cifrado, lê o valor **bruto** do storage e prova que **não contém o texto em claro** ('Maria'/'diabetes'), recuperável só com a chave. Teste, não inspeção.
- **AC4 (cripto trânsito):** `buildPgConfig` força `ssl.rejectUnauthorized=true` em host remoto e **proíbe `sslmode=disable`**; testes cobrem local/remoto/disable/require. A recusa *ao-vivo* de conexão sem TLS exige servidor real → **diferida ao deploy/POC** (PGlite é in-process); aqui é "verificação equivalente" por config, conforme o AC permite.
- **AC3 (migrations):** aplicação do zero + idempotência testadas no PGlite.
- **AC6 (integridade):** FK `USER 1—N CONSULTATION` e `UNIQUE` do CONSENT 1:1 testados (inserts inválidos rejeitados).
- **AC7 (residência):** follow-up registrado (comentário na migration + esta nota) → decidido na **Story 1.8**, não aqui.
- **Decisões YOLO:** (1) AES-GCM app-level em vez de pgcrypto (portável/testável sem extensão); (2) runner SQL próprio em vez de ORM (KISS, SQL versionado explícito); (3) `app_user` em vez de `user` (palavra reservada PG); (4) `@types/node` fixado em ^22 (runtime Node 22).
- **CodeRabbit:** não executado (exige `coderabbit auth login` interativo) → gate pre-PR (@devops). Sem CRITICAL conhecido. **Push** não realizado (autoridade @devops).
- **Coordenação com 1.2 (auth):** `app_user` é a fonte de verdade do schema USER (1.2 ainda não implementada); 1.2 estende, não duplica (IDS).

### File List

**Criados:**
- `packages/crypto/package.json`
- `packages/crypto/tsconfig.json`
- `packages/crypto/src/index.ts`
- `packages/crypto/src/aes-gcm.ts`
- `packages/crypto/src/aes-gcm.test.ts`
- `packages/db/package.json`
- `packages/db/tsconfig.json`
- `packages/db/src/index.ts`
- `packages/db/src/connection.ts`
- `packages/db/src/connection.test.ts`
- `packages/db/src/migrate.ts`
- `packages/db/src/pg-executor.ts`
- `packages/db/src/types.ts`
- `packages/db/src/migrations/0001_init.sql`
- `packages/db/src/schema.test.ts`

**Modificados:**
- `vitest.config.ts` (testTimeout 30s para o init do PGlite)
- `.env.example` (`DATABASE_URL`, `DATA_ENCRYPTION_KEY`)
- `pnpm-lock.yaml` (pg, pglite, @types/pg)

## QA Results

**Gate: CONCERNS → Done** (2026-06-11, Dex (@dev) — quality_gate da story). Arquivo: `docs/qa/gates/1.3-modelo-dados-base-criptografia.yml`. Re-verificação independente: 65/65 testes, lint, typecheck (9/9), build — todos PASS. Fundação de dados sólida; cripto-at-rest provada por teste (PGlite); migrations 0002/0003 aplicam sobre 0001 sem conflito. Pendentes: CodeRabbit pre-PR; AC4 TLS ao-vivo diferido ao deploy (E3).
