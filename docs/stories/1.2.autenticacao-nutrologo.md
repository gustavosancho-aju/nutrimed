# Story 1.2: Autenticação do Nutrólogo (Login/Sessão)

## Status

Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, test, build, coderabbit]

## Story

**As a** nutrólogo usuário do NutriMed,
**I want** autenticar com login e manter uma sessão segura,
**so that** apenas profissionais autorizados acessem o sistema e toda consulta/contribuição fique vinculada à minha identidade (pré-requisito para auditoria e compliance).

## Acceptance Criteria

1. O nutrólogo consegue autenticar (login) e o sistema estabelece e mantém uma sessão segura (ref. Critério de Aceitação 2 do E1).
2. Rotas/recursos protegidos exigem sessão válida; acesso sem sessão é negado e redirecionado para login.
3. A identidade do usuário autenticado fica disponível no servidor para vincular `CONSULTATION` e auditoria a um `USER` (architecture §8) — sem expor a implementação de auth ao domínio.
4. Logout encerra a sessão e invalida o acesso a recursos protegidos.
5. Credenciais/segredos de autenticação nunca são versionados nem logados; tokens/cookies de sessão usam atributos seguros (HttpOnly, Secure, SameSite) quando aplicável.
6. O fluxo cobre os estados de erro: credencial inválida, sessão expirada e acesso não autorizado, com mensagens claras (sem vazar detalhes sensíveis).

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Security (autenticação, gestão de sessão)
**Secondary Type(s)**: API (rotas protegidas), Frontend (telas de login/logout)
**Complexity**: Medium

### Specialized Agent Assignment

**Primary Agents**:
- @dev (executor)
- @architect (quality gate — valida o padrão de auth e o boundary domínio↔auth)

**Supporting Agents**:
- @devops (push/PR exclusivo; configuração de secrets de provider de auth, se aplicável)
- @qa (validação de story e cobertura dos fluxos de erro)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): rodar antes de marcar a story como Review
- [ ] Pre-PR (@devops): rodar antes de criar o pull request
- [ ] Pre-Deployment (@devops): N/A nesta story

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações), HALT se persistir
- HIGH issues: auto_fix na 1ª iteração, senão document_as_debt

### CodeRabbit Focus Areas

**Primary Focus**:
- Security: gestão de sessão, atributos de cookie (HttpOnly/Secure/SameSite), proteção de rotas
- Error handling: estados de credencial inválida / sessão expirada / não autorizado sem vazamento de detalhes

**Secondary Focus**:
- Secrets management: nenhuma credencial hardcoded
- Validação de input nos endpoints de auth

## Tasks / Subtasks

- [x] **Task 1 — Decidir e configurar o mecanismo de auth** (AC: 1, 3, 5)
  - [x] @architect decide a abordagem (provider gerenciado vs. sessão própria) registrando a decisão; @dev implementa atrás de um módulo de auth isolado do domínio (AC 3)
  - [x] Configurar variáveis/segredos via `.env.example` (sem valores reais)
- [x] **Task 2 — Fluxo de login/sessão** (AC: 1, 4)
  - [x] Endpoint/rota de login que estabelece sessão segura
  - [x] Persistência de sessão (cookie HttpOnly/Secure/SameSite ou equivalente do provider)
  - [x] Logout que encerra e invalida a sessão (AC 4)
- [x] **Task 3 — Proteção de recursos** (AC: 2, 3)
  - [x] Middleware/guard que exige sessão válida em rotas protegidas e redireciona para login
  - [x] Helper server-side `getCurrentUser()` que expõe a identidade ao domínio sem acoplá-lo ao provider (AC 3)
- [x] **Task 4 — Tratamento de erros e UI mínima** (AC: 6)
  - [x] Tela de login (shadcn/Tailwind) com estados de erro
  - [x] Mensagens para credencial inválida, sessão expirada, não autorizado
- [x] **Task 5 — Testes** (AC: 1, 2, 4, 6)
  - [x] Testes dos fluxos: login OK, login inválido, acesso protegido sem sessão, logout, sessão expirada

## Dev Notes

> Fontes: `docs/architecture.md` §8 (USER, CONSULTATION), §9 (stack), ADR-006 (compliance-by-design); `docs/epics/epic-01-fundacao-compliance.md` (IN: "Autenticação do nutrólogo (login/sessão)").

### Contexto e boundaries

- **Modelo de dados (architecture §8):** `USER ||--o{ CONSULTATION : conduz`. A auth desta story produz a identidade `USER` que stories seguintes (consulta, consent, auditoria) vinculam. **Esta story não cria o schema completo** — o modelo de dados base é a Story 1.3. Coordene: se a tabela `USER` ainda não existir, defina o mínimo necessário aqui e alinhe com 1.3 para evitar duplicação (IDS: REUSE > ADAPT > CREATE).
- **Boundary domínio↔auth (AC 3):** o domínio nunca importa o provider de auth diretamente; consome `getCurrentUser()` / identidade injetada. Isso preserva a abstração e facilita troca (espírito do NFR8 aplicado à auth).
- **Compliance (ADR-006):** auth é pré-condição de auditoria — toda escrita clínica precisa de um `USER` rastreável (NFR10, coberto na Story 1.5).
- **Stack:** Next.js 16+ / TS (architecture §9). Usar o padrão de auth idiomático ao Next escolhido pelo @architect.

### O que NÃO fazer nesta story

- Não implementar RBAC/multi-perfil além de "nutrólogo autenticado" (MVP — uma persona de usuário).
- Não construir o modelo de dados completo (Story 1.3) nem auditoria (Story 1.5).
- Não integrar EHR/SSO de terceiros (fora do MVP — épicos README §7).

### Boundary de agentes

- `git push`, PR e configuração de secrets no CI/provider = **@devops exclusivo**. @dev faz commits locais e delega push/PR.

### Testing

- **Localização/framework:** seguir o padrão estabelecido na Story 1.1 (Vitest/Jest, `*.test.ts`).
- **Cobertura mínima:** os 5 fluxos da Task 5. Priorizar testes de proteção de rota e expiração de sessão (segurança).
- **Não logar segredos/tokens** nos testes nem em fixtures.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-09 | v0.1 | Draft inicial a partir do E1 (Stories Candidatas #2) | River (@sm) |
| 2026-06-10 | v0.2 | Validated GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-06-10 | v0.3 | Implementação concluída (YOLO) — Status: Ready → InProgress → Ready for Review; @nutrimed/auth (scrypt+sessão), shell navegável (login→dashboard) via PGlite; e2e validado no browser; 30 testes PASS | Dex (@dev) |
| 2026-06-11 | gate | Quality gate CONCERNS — Status: Ready for Review → Done | Aria (@architect) |

## Dev Agent Record

### Agent Model Used

Dex (@dev) — Claude Opus 4.8 (1M context), `claude-opus-4-8`. Modo: YOLO.

### Debug Log References

- `pnpm install` (novo `@nutrimed/auth`) → OK.
- `pnpm lint`/`typecheck` → PASS (7 workspaces). `pnpm test` → 30 PASS (8 novos de auth). `pnpm build` → PASS (`/` dinâmica, `/login` estática).
- **Verificação e2e ao vivo (Playwright):** `pnpm dev` → `GET /` redireciona p/ `/login` (proteção OK) → login com a conta demo → cookie de sessão setado → `/` renderiza o dashboard "Bem-vinda, Dra. Demo". Screenshots conferidos (não versionados).

### Completion Notes List

- **Decisão de auth (YOLO, a ratificar por @architect):** sessão própria autocontida — **email+senha com hash scrypt** (`@nutrimed/auth`), **sessão persistida no banco** (tabela `session`, guarda só o SHA-256 do token), **cookie HttpOnly/SameSite=lax/Secure-em-prod**. Zero dependência externa; testável com PGlite. A abstração permite trocar por provider gerenciado (Auth.js/Supabase) depois sem tocar o domínio (AC3).
- **AC3 (boundary domínio↔auth):** domínio consome `getCurrentUser()` (apps/web/lib/auth.ts); nenhum acoplamento a provider.
- **AC2 (proteção):** rotas protegidas via checagem server-side + `redirect('/login')` (guard); verificado ao vivo.
- **AC4 (logout):** `logoutAction` deleta a sessão no banco e limpa o cookie; teste `deleteSession` cobre invalidação.
- **AC5 (segredos):** senha nunca em claro (scrypt salted); token só como hash no banco; cookie HttpOnly; `.env.example` documenta vars; nada versionado.
- **AC6 (erros):** tela de login com estado de erro ("Credenciais inválidas") via `useActionState`.
- **Shell navegável local SEM Docker:** `apps/web/lib/db.ts` usa **PGlite file-backed** (`.pgdata`, gitignored) em dev e `pg`+TLS em prod (`DATABASE_URL`); aplica migrations e faz **seed de usuário demo** (`demo@nutrimed.test` / `nutrimed123`) na 1ª init. `next.config`: `serverExternalPackages` p/ pg/pglite.
- **Refactor de migrations (em `@nutrimed/db`):** migrations agora são **inline em TS** (`migrations.ts`) em vez de ler `.sql` do disco — robusto ao bundling do Next. Adicionada migration `0002_auth_session` (coluna `password_hash` + tabela `session`). Removido `src/migrations/0001_init.sql`; teste do schema ajustado.
- **`app_user`:** estende o schema da Story 1.3 (não duplica USER) — IDS REUSE.
- **Disclaimer mínimo** (`components/disclaimer-note.tsx`) inserido no dashboard — a Story 1.7 formaliza o Atom (a11y/biblioteca).
- **CodeRabbit:** não executado (auth interativa) → pre-PR (@devops). **Push** não realizado (@devops).
- **Coberto além do mínimo:** o fluxo e2e completo (login→dashboard→identidade) foi validado no browser real, não só por unit tests.

### File List

**Criados (`@nutrimed/auth`):**
- `packages/auth/package.json`
- `packages/auth/tsconfig.json`
- `packages/auth/src/index.ts`
- `packages/auth/src/password.ts`
- `packages/auth/src/session.ts`
- `packages/auth/src/auth.test.ts`

**Criados (`apps/web`):**
- `apps/web/lib/db.ts`
- `apps/web/lib/auth.ts`
- `apps/web/lib/auth-actions.ts`
- `apps/web/components/disclaimer-note.tsx`
- `apps/web/app/login/page.tsx`

**Modificados:**
- `apps/web/app/page.tsx` (esqueleto → dashboard protegido)
- `apps/web/package.json` (deps: @nutrimed/auth, pg, @electric-sql/pglite, @types/pg)
- `apps/web/next.config.ts` (transpilePackages + serverExternalPackages)
- `packages/db/src/migrate.ts` (lê de `migrations.ts` inline, sem fs)
- `packages/db/src/migrations.ts` (**novo** — migrations inline 0001 + 0002)
- `packages/db/src/schema.test.ts` (assert dos nomes de migration)
- `.env.example` (já tinha DB; sem novas vars)
- `.gitignore` (`.pgdata/`, artefatos de screenshot)
- `pnpm-lock.yaml`

**Removidos:**
- `packages/db/src/migrations/0001_init.sql` (substituído por `migrations.ts` inline)

## QA Results

**Gate: CONCERNS → Done** (2026-06-11, Aria (@architect) — quality_gate da story). Arquivo: `docs/qa/gates/1.2-autenticacao-nutrologo.yml`. Re-verificação independente: 65/65 testes, lint, typecheck (9/9), build — todos PASS. Auth sólida (scrypt+timingSafe, token opaco com SHA-256 no banco, cookie httpOnly/secure/lax). Pendentes: CodeRabbit pre-PR (processual); scryptSync síncrono (low, pós-piloto).
