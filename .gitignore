

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# AIOX runtime (gerado, não versionar)
.aiox/environment-report.json
.aiox/environment-report.yaml
.aiox/project-status.yaml
.aiox/handoffs/

# Build outputs (monorepo / Next.js)
.next/
out/
.turbo/
*.tsbuildinfo

# Package managers
.pnpm-store/

# Banco local de desenvolvimento (PGlite file-backed) — inclui backups (.pgdata.bak-*)
.pgdata
.pgdata*/
**/.pgdata
**/.pgdata*/

# Artefatos de browser/screenshots (Playwright MCP)
.playwright-mcp/
/login.png
/dashboard.png

# Testing
coverage/
.nyc_output/

# Temp
tmp/
temp/
*.tmp

# Stray research artifact (não faz parte do código)
medrxiv_page.html

# Framework AIOX — instalado via CLI, não versionado neste repo de produto
.aiox-core/

# Configs locais de outras ferramentas de IA (não versionar)
.cursor/
.gemini/
.codex/
.antigravity/
.kimi/

# Dumps de backup (nunca versionar dados)
backups/
